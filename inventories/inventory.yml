---
all:
  hosts:
    automation:
  children:
    config_vm:
      hosts:
        ansible-vm.westeurope.cloudapp.azure.com:
      vars:
        automation: true
    target_vm:
      hosts:
        target-vm.westeurope.cloudapp.azure.com:
      vars:
        ansible_user: ansible
        automation: false
      
