---
- name: Manage credentials for Automation
  hosts: localhost
  become: true
  vars_files: 
    - ./vars/main.yml

  roles:
    - user_creation
    
    
- name: Manage credentials for Automation
  hosts: localhost #all
 # become: true
  remote_user: ansible
  vars_files: 
    - ./vars/main.yml

  roles:
    - ssh-keys
